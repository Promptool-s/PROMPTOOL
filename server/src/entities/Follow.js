/**
 * Contrato de datos de la tabla `follows` (esquema inferido del uso real
 * del frontend — UsuarioApp.jsx).
 */
class Follow {
    follower_id;    // quién sigue
    following_id;   // a quién sigue
    created_at;
}

export default Follow;
